the program takes two command-line arguments.

the first is either 'm' (to run with a min-heap) or 'M' (to run with a max-heap)

the second is the location of the file to use as initial input data

example usage:

  ./lab m data.txt
  ./lab M data.txt
